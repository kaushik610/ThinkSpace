_schema-version: "3.2"
ID: thinkspacezadthinkspace
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: thinkspacezadthinkspace-destination-content
  type: com.sap.application.content
  requires:
  - name: thinkspacezadthinkspace-destination-service
    parameters:
      content-target: true
  - name: thinkspacezadthinkspace-repo-host
    parameters:
      service-key:
        name: thinkspacezadthinkspace-repo-host-key
  - name: thinkspacezadthinkspace-uaa
    parameters:
      service-key:
        name: thinkspacezadthinkspace-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: thinkspacezadthinkspace_html_repo_host
          ServiceInstanceName: thinkspacezadthinkspace-html5-srv
          ServiceKeyName: thinkspacezadthinkspace-repo-host-key
          sap.cloud.service: thinkspacezadthinkspace
        - Authentication: OAuth2UserTokenExchange
          Name: thinkspacezadthinkspace_uaa
          ServiceInstanceName: thinkspacezadthinkspace-xsuaa-srv
          ServiceKeyName: thinkspacezadthinkspace-uaa-key
          sap.cloud.service: thinkspacezadthinkspace
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: thinkspacezadthinkspace-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: thinkspacezadthinkspace-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - thinkspacezadthinkspace.zip
      name: thinkspacezadthinkspace
      target-path: resources/
- name: thinkspacezadthinkspace
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: thinkspacezadthinkspace-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: thinkspacezadthinkspace-destination-service
    service-plan: lite
- name: thinkspacezadthinkspace-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: thinkspacezadthinkspace-xsuaa-srv
    service-plan: application
- name: thinkspacezadthinkspace-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: thinkspacezadthinkspace-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
